{
  "name": "upwork-extension",
  "version": "1.0.0",
  "description": "Upwork extension with TypeScript",
"license": "MIT",
"repository": {
    "type": "git",
    "url": "git+https://github.com/OWNER/upwork-extension.git"
},
"bugs": {
    "url": "https://github.com/OWNER/upwork-extension/issues"
},
"homepage": "https://github.com/OWNER/upwork-extension#readme",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "dev": "pnpm run watch",
    "clean": "if exist dist rmdir /S /Q dist & if exist dist.zip del /Q dist.zip",
    "copy:assets": "mkdir dist\\src\\assets 2>nul && copy /Y src\\assets\\* dist\\src\\assets\\ && mkdir dist\\src\\popup 2>nul && copy /Y src\\popup\\* dist\\src\\popup\\",
    "zip:dist": "tar -a -c -f dist.zip -C dist .",
    "build:prod": "pnpm run clean && pnpm run build && pnpm run copy:assets && copy /Y manifest.json dist\\manifest.json && pnpm run zip:dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@types/node": "^24.3.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
